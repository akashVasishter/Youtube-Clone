import react from "react";

const Buttons = ({name}) => {

    return(

        <div>
        <button className="px-5 m-3 bg-gray-200 rounded-lg">{name}</button>   
       </div>
    )
    
}


export default Buttons;